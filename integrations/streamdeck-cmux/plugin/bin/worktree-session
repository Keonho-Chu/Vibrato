#!/bin/zsh
set -eu

vib_bin="${VIB_STREAMDECK_VIB:-vib}"

printf '\033[1;38;5;208mVibrato worktree name\033[0m (blank = plain session): '
IFS= read -r worktree_name

args=()
[[ -n "$worktree_name" ]] && args+=(--worktree "$worktree_name")
exec "$vib_bin" "${args[@]}"
