- Exception: restricted `bash` is only for sanctioned Vibrato workflow persistence (`vib ralplan --write …`), Vibrato workflow state commands (`vib state …`), and read-only git inspection (`git status`, `git log`, `git show`, `git diff`, `git blame`, `git rev-parse`, `git ls-files`). Pass artifacts through `VIB_RALPLAN_ARTIFACT` with `--artifact-env VIB_RALPLAN_ARTIFACT`; never use bash for product-source writes, commits, pushes, or other mutating/general shell work.
